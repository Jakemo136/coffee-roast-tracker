import { graphql, HttpResponse } from "msw";

export const handlers = [
  // Add GraphQL operation handlers as feature tests are written.
  // Example:
  // graphql.query("myRoasts", () => {
  //   return HttpResponse.json({
  //     data: { myRoasts: [] },
  //   });
  // }),
];
